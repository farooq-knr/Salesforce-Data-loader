# Salesforce credentials (username + password + security token)
SF_USERNAME=
SF_PASSWORD=
SF_SECURITY_TOKEN=
# Set to 'login' for production, 'test' for sandbox
SF_DOMAIN=login
# Optional: API version or client id if needed
# SF_CLIENT_ID=your_client_id
